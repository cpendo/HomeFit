# Backend API base URL (no trailing slash, no /api suffix — slices append their own paths)
VITE_API_BASE_URL=http://localhost:5000

# Set to "true" to display the demo-mode banner on the login screen.
# Should be "true" on the production portfolio deploy.
VITE_DEMO_MODE=false
